# # Import the properties file located one directory up
# spring.config.import=optional:file:../../../env.properties

# Define application-specific properties using the imported values
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
security.jwt.secret-key=${JWT_SECRET}
spring.security.enabled=false


# server.error.include-stacktrace=never
# spring.datasource.url=jdbc:h2:file:./data/books;DB_CLOSE_ON_EXIT=FALSE;AUTO_RECONNECT=TRUE;NON_KEYWORDS=USER
# spring.datasource.driver-class-name=org.h2.Driver
# spring.h2.console.enabled=true